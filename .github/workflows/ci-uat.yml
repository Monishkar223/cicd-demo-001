name: CI - UAT to Main (PR Request)

on:
  push:
    branches:
      - uat

jobs:
  create-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Create PR uat → main
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: main
          branch: uat
          title: "UAT → Main (Approval Required)"
          body: |
            ✅ Code pushed to UAT successfully.
            🔍 Please review and approve to merge into MAIN.
